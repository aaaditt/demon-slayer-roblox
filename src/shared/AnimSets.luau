-- Roblox-side registry of animation sets by name (model attribute "AnimSet").
local AnimData = require(script.Parent.AnimData)
return {
    Tanjiro = AnimData.prepare(require(script.Parent.Anims.Tanjiro)),
}
